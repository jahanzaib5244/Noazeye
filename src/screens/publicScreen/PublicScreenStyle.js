import {  StyleSheet } from 'react-native'

import {size ,Colors , font} from  '../../config/Utils';

export const PublicScreenStyle = StyleSheet.create({
    root:{
        flex:1,
        backgroundColor:Colors.primary,
    },
  
})
